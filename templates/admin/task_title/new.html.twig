{% extends 'base.html.twig' %}

{% block title %}New Task Title{% endblock %}

{% block content %}
    <h1>Create new Task Title</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ include('admin/task_title/_form.html.twig') }}
        <button class="btn btn-primary">Save</button>
        <a href="{{ path('admin_task_title_index') }}" class="btn btn-link">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
