{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block content %}
    <h1>User</h1>

    {{ include('admin/user/_detail.html.twig') }}
    <a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="btn btn-primary">Edit</a>
    <a href="{{ path('admin_user_index') }}" class="btn btn-link">Back</a>
{% endblock %}
