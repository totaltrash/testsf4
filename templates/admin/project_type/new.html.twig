{% extends 'base.html.twig' %}

{% block title %}New Project Type{% endblock %}

{% block content %}
    <h1>Create new Project Type</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ include('admin/project_type/_form.html.twig') }}
        <button class="btn btn-primary">Save</button>
        <a href="{{ path('admin_project_type_index') }}" class="btn btn-secondary">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
