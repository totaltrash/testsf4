{% extends 'base.html.twig' %}

{% block title %}Delete Project Type{% endblock %}

{% block content %}
    <h1>Delete Project Type</h1>

    {{ include('admin/project_type/_detail.html.twig') }}
    <form method="POST">
        <button class="btn btn-danger" type="submit">Confirm Delete</button>
        <a href="{{ path('admin_project_type_show', {'id': project_type.id}) }}" class="btn btn-secondary">Cancel</a>
    </form>
{% endblock %}
