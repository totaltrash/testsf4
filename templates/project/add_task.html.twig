{% extends 'base.html.twig' %}

{% block title %}Add Task To Project{% endblock %}

{% block content %}
    <h1>Add Task To Project</h1>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ include('task/_form.html.twig') }}
        <button class="btn btn-primary">Save</button>
        <a href="{{ path('project_show', {'id': project.id}) }}" class="btn btn-link">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
