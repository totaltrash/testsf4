{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}

{% block content %}
    <h1>Project</h1>

    {{ include('project/_detail.html.twig') }}
    <a href="{{ path('project_edit', {'id': project.id}) }}" class="btn btn-primary">Edit</a>
    <a href="{{ path('project_delete', {'id': project.id}) }}" class="btn btn-danger">Delete</a>
    <a href="{{ path('project_index') }}" class="btn btn-secondary">Back</a>
{% endblock %}
