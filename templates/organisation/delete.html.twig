{% extends 'base.html.twig' %}

{% block title %}Delete Organisation{% endblock %}

{% block content %}
    <h1>Delete Organisation</h1>

    {{ include('organisation/_detail.html.twig') }}
    <form method="POST">
        <button class="btn btn-danger" type="submit">Confirm Delete</button>
        <a href="{{ path('organisation_show', {'id': organisation.id}) }}" class="btn btn-link">Cancel</a>
    </form>
{% endblock %}
